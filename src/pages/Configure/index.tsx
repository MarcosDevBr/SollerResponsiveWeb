import React from "react";
import { IConfigure } from "./model";
import { ConfigureContainer } from "./style";

export default function Configure(_props: IConfigure) {
    return (
        <ConfigureContainer>
            Configure Page
        </ConfigureContainer>
    )
}