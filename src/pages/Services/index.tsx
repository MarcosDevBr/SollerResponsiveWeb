import React from "react";
import { IServices } from "./model";
import { ServicesContainer } from "./style";

export default function Services(_props: IServices) {
    return (
        <ServicesContainer>
            Services Page
        </ServicesContainer>
    )
}