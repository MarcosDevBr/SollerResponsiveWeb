import { IContent } from "./model";
import { ContentContainer } from "./style";

export default function Content(_props: IContent) {
    return (
        <ContentContainer>
            Content
        </ContentContainer>
    )
}