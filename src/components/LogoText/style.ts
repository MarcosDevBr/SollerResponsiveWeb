import styled from 'styled-components'

const LogoTextConteiner = styled.div`
    height: 36px;
    gap: 0px;
    opacity: 0px;
    
    span {
    /* font-family: Roboto; TODO: check if you are going to use this font */
        font-size: 32px;
        font-weight: 700;
        line-height: 35.2px;
        text-align: left;
        color: #0F172A;
        width: 81px;
    }
`

export {
    LogoTextConteiner
}